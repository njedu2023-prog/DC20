# DC20 2026-09-05 修复与混合盈利升级阶段记录

受审起点：`c26405b044cae2e771577f1dfbc9b8f1db073043`。

## 本次落地

1. Verify 按实际使用的冻结 P0/P1/Shadow 合同验证：活动模型冻结、源码 pin、exact base、文件哈希、完整候选和严格 SSE 日期均保留。独立名单不再被旧 Auction Action 的错日期重放阻断。历史 Auction 回放和 Core 独立重放仍保留。
2. 自然 Verify 补取已有冻结 Shadow 样本所缺的 exact-T 竞价及退出行情。只使用专用 Tushare endpoint，不造名单、不写 Action、不覆盖已有真值、不将缺竞价降级为日线。缺权限、无数据、错误日期或缺字段均保持 pending。
3. 修复旧 Shadow 结算 shell heredoc 缩进错误；新增全部 shell 片段语法测试。
4. 核心指标与观察统计切换至 SHA 绑定的全部冻结 P0 TopN。统计始于 D=20260828；回溯恢复样本单独排除。读取失败、未到期、到期缺真值和退出受阻分别显示，不能用全 0 掩盖。
5. 混合盈利离线研究比较两种固定收益/下行候选。报告和结果在 `work/executable-profit-upgrade-20260905`。新候选没有通过替换标准，生产模型字节未修改。

## 统计边界

晋级预测在 D 日已经冻结，其后验证可以检验该前向预测。新显示的收益是对既有冻结名单的**事后日线代理观察**；口径在2026-09-05定义，不能称从8月28日已前向运行的新收益策略，不能与严格竞价 Shadow 账本混为一谈。

日线代理成本为45bp；开盘涨停不臆测买入，从严格 T+1 起采用首个可交易开盘退出。缺少中间行情不跳过，跌停或无量不臆测退出，未完成收益保持空值。存在到期未完成样本或顺延退出资本重叠时，不报告完整窗口累计收益/回撤；不把只结算了部分日期的复合值当作资金净值。

本次公开观察快照截至严格交易日20260904：4个自然D、32个样本，26条T真值；19个已结算槽中16个代理成交、3个未买空槽，1个退出待核。20个回溯恢复样本不进入此前向预测观察范围。代码、名称、原晋级名次保持不变。

## 尚待自然运行验收

真实 Tushare 竞价补取及正式 Verify 的候选、CAS、Pages 闭环须由修复后的自然 Verify 验收。本次没有调用交易接口，没有普通重跑或提前派发失败 run，没有伪造竞价真值。不能把本地测试通过或日线观察表有数字，宣称为严格 Shadow 已完成结算。
