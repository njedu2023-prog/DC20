# V2 实际归档验收 — 2026-09-12

## 事实与版本

- 研究运行：[34676871475](https://github.com/njedu2023-prog/DC20/actions/runs/34676871475)。结论：failure。
- 运行源码：`df6c38806da20066f673875a4a83c37fa895d2e4`。
- 归档：`10292829634`，25,982,564 字节。
- ZIP SHA256：`58467518002c587349587eebb32681b1d81c3c8850ca5595b342818157ebfc64`。下载后与 GitHub artifact digest 完全一致。
- 新只读验收工具通过完整性检查：7,339 个文件成员、170,525,054 字节解压内容、7,330 个绑定数据来源、19 个运行代码来源。
- 完整性通过不等于研究通过、交易可执行、模型有效或可以上线。

## 实际候选与退出结果

固定范围没有变化：6,753 个候选、910 个 D 日（2022-11-11 至 2026-08-14），行情截止 2026-09-11。

| 结果状态 | 候选数 |
| --- | ---: |
| 10:00 / 封板延持新规则已结算的分钟价格代理 | 2,485 |
| 开盘涨停，无法确认竞价成交，保留未成交状态 | 1,001 |
| 尚缺合格的 canonical 竞价来源 | 3,265 |
| 持有期间缺日线 | 2 |

不能用前两组替代完整候选回测：缺证据部分与日期有关，并非随机缺失。模型训练和资金对比仍处于 `BLOCKED_COLLECTION_EVIDENCE`，没有训练新盈利模型，也没有可报告的完整新策略收益。

两条缺日线记录均来自 D 2024-04-25：`600083.SH` 缺 2024-04-30；`600234.SH` 缺 2024-04-29。不能从“文件没有该股”自动推断停牌或零收益。

## 采集结果及拒绝位置

- 实际 API 调用 3,119 次；总耗时约 1,932 秒。
- 2,726 份分钟请求成功写入原始数据表及来源凭证。
- 517 个早于 canonical 接口登记覆盖起点的 T 日，保留原计划允许的覆盖前声明。
- 393 个覆盖期内的竞价请求中，10 个返回合格空表；383 个在响应校验阶段被拒绝。
- 所有这 383 条均是 `PENDING_INVALID_RESPONSE_NOT_IMPUTED`，不是已记录的权限拒绝、限流、网络错误或预算耗尽。
- 首个失败日期 2025-01-16；末个 2026-08-17。两者均收到非空 HTTP 响应（277,536 / 307,196 字节），但没有形成合格来源文件。

旧采集器在校验失败时只记录通用状态和响应 SHA，没有保留具体异常枚举或原始数据表，所以尚不能从归档确认究竟是哪一条校验失败。

## 已核实的线索，不是已确认根因

当前冻结竞价编码器逐行要求 `abs(price * vol - amount) <= 0.02`，任一行失败都会拒绝整个全市场表。

[Tushare 官方 stk_auction 文档](https://tushare.pro/document/2?doc_id=369) 的示例自身存在超过该阈值的差异：

- `600879.SH`：195,900 × 9.190 − 1,800,320 = 1 元。
- `300616.SZ`：142,700 × 14.370 − 2,050,600 = −1 元。
- `300949.SZ`：25,600 × 41.210 − 1,054,980 = −4 元。

这是验证口径可能过严的证据，但不证明本次所有失败都由这一原因造成，也不自动证明任何价格或成交容量正确。

## 后续控制

仅新增两个固定日期、原参数与原字段的诊断请求，保留原始数据表和允许列出的校验错误枚举，定位实际行级触发原因。不重跑整批、不将校验失败当作竞价不可用、不把缺证据记零、不修改旧归档。

若确认为金额精度问题，后续必须单独版本化来源规则，区分价格资格与容量资格；需重新审查候选逐行证据、保持原价格和名次、从已绑定归档复用合格日线与分钟，不能直接在旧 v2 结果上改状态或放宽旧门槛。

一分钟执行延迟检查工具已完成软件测试，但实际完整报告仍受同一竞价来源门槛约束，不把部分已结算样本的均值当作完整策略表现。
