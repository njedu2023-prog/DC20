# 10:00 / 封板延持盈利模型升级

本目录是与正式榜单、模型、影子账本隔离的可复现升级研究入口。
不能因为测试通过、采集成功或训练完成就宣布盈利提升或替换正式权重。

## 按顺序执行

1. `probe.py`：现有凭据最多两次官方分钟读取。2026-09-12 实测
   `600000.SH` 的 20260911、20221114 均通过完整 240 分钟校验。
   [探针运行](https://github.com/njedu2023-prog/DC20/actions/runs/34670144882)。
2. `run.py --output ... --source-zip ... --tail-zip ...`：验证两份完整 ZIP、
   manifest 和各文件 SHA；只导入原始日线/涨跌停证据；原失败记录保留。
   严格复用 6753 个历史候选、910 个 D（20221111–20260814）及 D 日特征。
   不复制旧开盘退出收益，不把回溯重建当作自然前向预测。
3. `collect.py`：先尝试 T 日竞价真值，再按新规则重放结果，仅采集实际缺失的
   股票/交易日分钟证据。固定 9000 次 API、90 分钟上限、最多 4 并发及每秒 2 请求。
   失败、无权限、空数据、缺分钟均保留回执，不填补为成交或零收益。
4. `run.py --evaluate-root ...`：用同一个正式退出计算引擎重建标签。
   按实际退出成熟日期净化训练时间边界；完整日期门槛通过才拟合预先固定的 Ridge。
   训练截止 20251111；之后至 20260814 为已经研究过的开发验证段，不是独立测试。
   Top1 和 Top2 每日均选，负评分或负收益不剔除。
5. 保存候选、逐行预测、数据覆盖与比较结果；禁止自动写入正式权重。
   20260914 起数据预留作前向留出，不可倒灌开发调参。
6. `capital_report.py --root ...`：独立重建标签和固定候选，再对同一验证日期的
   候选排名、晋级排名分别回放 Top1 / Top2 两个独立 100 万账户。
   每笔 10 万、100 股取整；09:25 买入不能提前使用当天 10:00 才退出的资金。
   买入资金不足、未成交、缺行情及亏损均保留；缺少真实估值时不伪造资金净值。
   此入口不读旧收益作为真值，不请求行情接口，不改模型或正式账本。

第一轮历史采集运行结束后，`research_profit_1000_capital.yml` 已自动执行第 6 步，
但完整性门槛未通过，未产生新训练模型或可用的完整资金净值。
它只接受已固定的上游 run `34671477608` 和 commit
`4675fab984050fa32be875fff07e0285e8903ebb`，仅有仓库与 artifact 读取权限，
没有行情凭据或交易权限。上游资料不完整时，继续保存阻断原因，不伪造成功训练或净值。

## 利用原晋级模型

原模型、冻结名单及连板路径展示不改动。历史训练仅使用训练截止早于 D 的 OOF
晋级排名作为新盈利模型输入，并与同候选池晋级排名基线比较。
历史缺少晋级概率、命名连板路径及同历史训练截止的正式盈利排名，因此保持缺失，
不能拿现在的模型回填历史预测。已有 D 日走势与阶段特征保留。

## 仍需验收的边界

- 竞价成交价/开盘价、下一分钟开盘退出均是价格代理，不等于账户实际成交。
  开盘涨停无法确认排队成交、容量不足、跌停不能退出、停牌等状态要保留。
- 新研究按用户最终竞价价指令使用无额外 D 日限价的买入政策；当前正式影子账本
  仍可能受冻结限价约束。两者有明确不同政策 ID，未对齐前不能宣称同口径上线。
- 45bp 为既定成本假设；90bp 压力成本需要同一持仓/成交记录再计算。
  不能用负收益过滤改善胜率，也不能把未完成日期剔除后称为完整资金净值。
- 100 万总资金、每笔 10 万、封板延持造成的资金占用，必须通过重叠持仓资金账
  验收后才能报告资金约束下的研究结果；候选报告中的槽位收益不是账户净值。
  `capital.py` 的纯函数要求调用者验证标签；公开仓库适配器必须从候选 manifest
  重新生成标签、核验价格来源 SHA，不能接受手写或篡改的买入价与收益。
- 新模型至少需要新规则数据完整性、时间顺序比较、前向留出验证和独立上线审查。
  这些步骤未通过时保留原权重，负结果也保留。

大体积市场证据仅存隔离研究 artifact（30 天保留），不塞进正式 HTML 或生产数据。
下载归档并长期留存前应校验 artifact digest 和内部 source SHA。

## 旧日线缺口诊断

旧归档的完整分页响应中有五个候选在首次退出日没有日线，之后恢复成交。
`suspension_probe.py` 固定核查这五对代码/日期及相邻停牌记录，最多十次请求：
600234.SH / 20240429、603226.SH / 20250610、603122.SH / 20251117、
603580.SH / 20260707、002036.SZ / 20260723。这只是证据诊断，不能凭空数据
推定停牌，不能合成零成交 OHLC，更不能直接据此结算收益。是否影响训练门槛，
还取决于这些候选经实际竞价与容量规则核验后是否属于买入样本。

## 第一轮实际结果与价格取证

历史运行 `34671477608` 的 6753 条中，1109 条完成新规则价格代理结算，
3249 条被竞价价/日线开盘价冲突拦住，568 条等待分钟真值，3 条等待退出日线，
其余为未成交代理判定。训练完整日门槛没有通过；没有训练或发布新模型。
已保存原始研究 artifact，整包 SHA256 为
`d004f6decba35d6148082333764ba0988bc3fa25062224492d485ac031fe2a29`。

3249 条冲突的既有买价均取 `stk_auction_o.close`，而该接口 `open` 与日线
`open` 一致。不能仅以相等为由自动改字段：`price_probe.py` 固定四个真实股票日，
对 `daily`、`stk_auction_o`、`stk_auction` 最多做十二次只读请求，对照不同
接口的原始值。每份数据、请求和 HTTP 响应摘要分别绑定；不保留任意服务端消息，
不替代买入价、不改标签、不购买接口权限。接口拒绝、缺数据和冲突也保留。
官方 `stk_auction` 文档的历史范围自 2025 年 1 月起，较早日期作为缺源对照。
后续源政策修复、数据补采、重新训练与上线仍需分别验收，不能跳过完整性门槛。

第二阶段的来源诊断、独立适配器及尚未完成的接线/重放步骤见
[SOURCE_POLICY_V2.md](SOURCE_POLICY_V2.md)。新适配器尚未接入正式模型、账本或旧研究运行；
原计划与原始研究结果保留，不能将这些准备工作描述为已完成重新训练。

参考：[开盘竞价 OHLC 接口](https://tushare.pro/document/2?doc_id=353)、
[竞价成交信息接口](https://tushare.pro/document/2?doc_id=369)、
[历史分钟接口](https://tushare.pro/document/2?doc_id=370)。
