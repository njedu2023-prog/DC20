name: DC20 controlled D22 formal projection recovery
on:
  push:
    branches: [main]
    paths: [.github/workflows/recover_publication_20260922.yml]
permissions:
  contents: read
concurrency:
  group: dc20-publication-recovery-20260922
  cancel-in-progress: false
jobs:
  dispatch:
    if: github.repository == 'njedu2023-prog/DC20' && github.ref == 'refs/heads/main' && github.run_attempt == 1
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      actions: write
      contents: read
    steps:
      - name: Dispatch existing publisher only after original observer success
        env:
          GH_TOKEN: ${{ github.token }}
        shell: python
        run: |
          import datetime,json,os,urllib.request
          root='https://api.github.com/repos/njedu2023-prog/DC20'
          def api(path,body=None):
              data=None if body is None else json.dumps(body).encode()
              request=urllib.request.Request(root+path,data=data,headers={'Authorization':'Bearer '+os.environ['GH_TOKEN'],'Accept':'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28'})
              with urllib.request.urlopen(request,timeout=30) as response:
                  raw=response.read()
              return json.loads(raw) if raw else None
          if datetime.datetime.now(datetime.timezone.utc)>=datetime.datetime(2026,9,23,1,20,tzinfo=datetime.timezone.utc):
              raise SystemExit('D22 formal publication cutoff expired')
          run=api('/actions/runs/35766623062')
          assert run['status']=='completed' and run['conclusion']=='success' and run['run_attempt']==1
          assert run['workflow_id']==357027830 and run['head_branch']=='main'
          assert run['path']=='.github/workflows/research_candidate_natural_observer.yml'
          assert run['head_sha']=='8d607564417e29af9152bc8646b3afc71d3a9d49'
          assert run['repository']['full_name']==run['head_repository']['full_name']=='njedu2023-prog/DC20'
          previous=api('/actions/workflows/recover_publication_20260922.yml/runs?per_page=100')['workflow_runs']
          if any(r['id']!=int(os.environ['GITHUB_RUN_ID']) and r['conclusion']=='success' for r in previous):
              print('Publication recovery already dispatched; no duplicate')
          else:
              api('/actions/workflows/publish_candidate_profit.yml/dispatches',{'ref':'main','inputs':{'dry_run':False}})
              print('Dispatched existing provenance-validating publisher; no model or snapshot override')
