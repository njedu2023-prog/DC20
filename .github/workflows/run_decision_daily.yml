name: Run Top10 Decision (Auto Daily)

on:
  schedule:
    - cron: "15 13 * * 1-5"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "只读演练；完成真实同步、计算和校验但不提交"
        required: true
        default: true
        type: boolean
      trade_date:
        description: "手动指定交易日 YYYYMMDD（留空=由当前交易所会话严格解析）"
        required: false
        type: string

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONHASHSEED: "0"
  OMP_NUM_THREADS: "1"
  OMP_THREAD_LIMIT: "1"
  OMP_DYNAMIC: "FALSE"
  OPENBLAS_NUM_THREADS: "1"
  OPENBLAS_CORETYPE: "Haswell"
  GOTO_NUM_THREADS: "1"
  MKL_NUM_THREADS: "1"
  MKL_DYNAMIC: "FALSE"
  BLIS_NUM_THREADS: "1"
  VECLIB_MAXIMUM_THREADS: "1"
  NUMEXPR_NUM_THREADS: "1"
  NPY_ENABLE_CPU_FEATURES: "X86_V3"

concurrency:
  group: decision-auction-main-writer
  cancel-in-progress: false

jobs:
  compute:
    name: Read-only build and validate Daily Decision candidate
    if: ${{ github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main' }}
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    outputs:
      publish: ${{ steps.mode.outputs.publish }}
      has_changes: ${{ steps.candidate.outputs.has_changes || 'false' }}
    steps:
      - name: Checkout immutable main base without writer credentials
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          ref: main
          fetch-depth: 1
          persist-credentials: false
      - name: Resolve dry-run mode and immutable base
        id: mode
        env:
          INPUT_DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -euo pipefail
          publish=true
          if [ "${GITHUB_EVENT_NAME}" = workflow_dispatch ] && [ "${INPUT_DRY_RUN:-true}" != false ]; then publish=false; fi
          git rev-parse HEAD > "${RUNNER_TEMP}/base_sha.txt"
          echo "publish=${publish}" >> "${GITHUB_OUTPUT}"
      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: "3.12.13"
          cache: pip
          cache-dependency-path: requirements.lock
      - name: Install locked dependencies
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check --only-binary=:all: --require-hashes -r requirements.lock
          python -c 'import pip; assert pip.__version__ == "26.2.1", pip.__version__'
          python -m pip check
      - name: Require active production freeze and enforced pins for real publication
        env:
          PUBLISH: ${{ steps.mode.outputs.publish }}
          PYTHONPATH: ${{ github.workspace }}/src
        run: |
          python - <<'PY'
          import os
          from pathlib import Path
          from top10decision.decision.model_freeze import load_model_freeze, model_freeze_active, validate_pinned_files
          root=Path('.').resolve(); manifest=load_model_freeze(root, required=True)
          active=model_freeze_active(manifest)
          if os.environ['PUBLISH'] == 'true' and not active: raise SystemExit('real Daily publication requires active Decision freeze')
          audit=validate_pinned_files(root, manifest, force_enforcement=not active)
          if audit.get('enforced') is not True: raise SystemExit('Daily preflight did not enforce frozen pins')
          PY
      - name: Validate optional manual trade date
        env:
          TRADE_DATE: ${{ inputs.trade_date }}
        run: |
          set -euo pipefail
          if [ -n "${TRADE_DATE}" ]; then echo "${TRADE_DATE}" | grep -Eq '^[0-9]{8}$'; fi
      - name: Require persisted Auction action for real Daily publication
        id: persisted_action
        env:
          PUBLISH: ${{ steps.mode.outputs.publish }}
          PYTHONPATH: ${{ github.workspace }}/src
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib, json, os
          from datetime import datetime, timedelta
          from pathlib import Path
          from top10decision.decision.action_plan import (
              action_plan_semantic_comparison_profile_v3,
              action_plan_semantic_projection_v3,
          )
          from top10decision.decision.canonical_fingerprint import canonical_json_bytes
          from top10decision.decision.model_freeze import (
              load_model_freeze,
              model_freeze_active,
              validate_action_plan_artifact,
          )

          def reject_duplicate_keys(pairs):
              result = {}
              for key, value in pairs:
                  if key in result:
                      raise ValueError('duplicate JSON key')
                  result[key] = value
              return result

          publish = os.environ['PUBLISH'] == 'true'
          if not publish:
              with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
                  output.write(
                      'validated=false\nsimulation=true\nsemantic_sha256=\n'
                      'comparison_profile=\nsignal_date=\nreport_date=\n'
                  )
          else:
              root = Path('.').resolve()
              try:
                  manifest = load_model_freeze(root, required=True)
                  audit = validate_action_plan_artifact(
                      root,
                      manifest,
                      force_enforcement=not model_freeze_active(manifest),
                  )
                  path = root / 'outputs/decision/action_plan_latest.json'
                  if (
                      audit.get('validated') is not True
                      or audit.get('enforced') is not True
                      or not path.is_file()
                      or path.is_symlink()
                  ):
                      raise RuntimeError('persisted Auction action was not enforced')
                  action = json.loads(
                      path.read_text(encoding='utf-8'),
                      object_pairs_hook=reject_duplicate_keys,
                  )
                  if not isinstance(action, dict):
                      raise RuntimeError('persisted Auction action must be an object')
                  generated_at = action.pop('generated_at_utc', None)
                  generated_time = datetime.fromisoformat(generated_at)
                  if (
                      type(generated_at) is not str
                      or generated_time.isoformat() != generated_at
                      or generated_time.utcoffset() != timedelta(0)
                      or generated_time.microsecond != 0
                  ):
                      raise RuntimeError('persisted Auction action timestamp is invalid')
                  signal_date = action.get('signal_date')
                  if type(signal_date) is not str:
                      raise RuntimeError('persisted Auction action signal_date is invalid')
                  try:
                      parsed_signal_date = datetime.strptime(signal_date, '%Y%m%d')
                  except ValueError as exc:
                      raise RuntimeError(
                          'persisted Auction action signal_date is invalid'
                      ) from exc
                  if parsed_signal_date.strftime('%Y%m%d') != signal_date:
                      raise RuntimeError('persisted Auction action signal_date is invalid')
                  report_date = action.get('report_date')
                  if type(report_date) is not str:
                      raise RuntimeError('persisted Auction action report_date is invalid')
                  try:
                      parsed_report_date = datetime.strptime(report_date, '%Y%m%d')
                  except ValueError as exc:
                      raise RuntimeError(
                          'persisted Auction action report_date is invalid'
                      ) from exc
                  if (
                      parsed_report_date.strftime('%Y%m%d') != report_date
                      or parsed_signal_date >= parsed_report_date
                      or action.get('exec_date') != report_date
                  ):
                      raise RuntimeError('persisted Auction action report_date is invalid')
                  comparison_profile = action_plan_semantic_comparison_profile_v3(
                      action
                  )
                  semantic_action = action_plan_semantic_projection_v3(
                      action,
                      comparison_profile=comparison_profile,
                  )
                  semantic_digest = hashlib.sha256(
                      canonical_json_bytes(semantic_action)
                  ).hexdigest()
              except Exception:
                  print(json.dumps({
                      'component': 'daily_auction_action_prerequisite',
                      'reason_code': 'AUCTION_ACTION_PREREQUISITE_FAILED',
                      'status': 'fail',
                  }, separators=(',', ':'), sort_keys=True))
                  raise SystemExit(1)
              with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
                  output.write(
                      'validated=true\nsimulation=false\n'
                      f'semantic_sha256={semantic_digest}\n'
                      f'comparison_profile={comparison_profile}\n'
                      f'signal_date={signal_date}\n'
                      f'report_date={report_date}\n'
                  )
          PY
      - name: Rebuild exact-base frozen Auction runtime before live source mutation
        id: frozen_action
        env:
          PERSISTED_ACTION_SEMANTIC_SHA256: ${{ steps.persisted_action.outputs.semantic_sha256 }}
          PERSISTED_ACTION_COMPARISON_PROFILE: ${{ steps.persisted_action.outputs.comparison_profile }}
          PERSISTED_ACTION_SIGNAL_DATE: ${{ steps.persisted_action.outputs.signal_date }}
          PERSISTED_ACTION_REPORT_DATE: ${{ steps.persisted_action.outputs.report_date }}
          PERSISTED_ACTION_VALIDATED: ${{ steps.persisted_action.outputs.validated }}
          PUBLISH: ${{ steps.mode.outputs.publish }}
          PYTHONPATH: ${{ github.workspace }}/src
        run: |
          set -euo pipefail
          replay_report="${RUNNER_TEMP}/daily-frozen-replay.json"
          replay_stdout="${RUNNER_TEMP}/daily-frozen-replay.stdout"
          replay_args=(
            --root "${GITHUB_WORKSPACE}"
            --report "${replay_report}"
          )
          if [ "${PUBLISH}" = true ]; then
            if [[ ! "${PERSISTED_ACTION_SIGNAL_DATE}" =~ ^20[0-9]{6}$ ]]; then
              echo 'Daily persisted Auction action signal_date is invalid' >&2
              exit 1
            fi
            if [[ ! "${PERSISTED_ACTION_REPORT_DATE}" =~ ^20[0-9]{6}$ ]]; then
              echo 'Daily persisted Auction action report_date is invalid' >&2
              exit 1
            fi
            replay_args+=(
              --signal-date "${PERSISTED_ACTION_SIGNAL_DATE}"
              --report-date "${PERSISTED_ACTION_REPORT_DATE}"
            )
          fi
          set +e
          python scripts/run_deterministic_numeric.py scripts/replay_frozen_canonical_v2.py \
            "${replay_args[@]}" > "${replay_stdout}"
          replay_status=$?
          set -e
          if [ "${replay_status}" -ne 0 ]; then
            REPLAY_REPORT="${replay_report}" python - <<'PY'
          import hashlib, json, os
          from pathlib import Path

          report = Path(os.environ['REPLAY_REPORT'])
          error = ''
          if report.is_file():
              try:
                  payload = json.loads(report.read_text(encoding='utf-8'))
              except (OSError, UnicodeError, json.JSONDecodeError):
                  payload = {}
              if isinstance(payload, dict) and isinstance(payload.get('error'), str):
                  error = payload['error']
          lowered = error.lower()
          if 'pinned' in lowered:
              reason = 'PIN_DRIFT'
          elif 'action' in lowered or 'watchlist' in lowered:
              reason = 'ACTION_WATCHLIST_DRIFT'
          elif 'canonical' in lowered:
              reason = 'CANONICAL_RUNTIME_DRIFT'
          elif 'history' in lowered or 'snapshot' in lowered:
              reason = 'FROZEN_HISTORY_DRIFT'
          else:
              reason = 'FROZEN_RUNTIME_VALIDATION_FAILED'
          print(json.dumps({
              'component': 'daily_frozen_replay',
              'error_sha256': hashlib.sha256(error.encode('utf-8')).hexdigest(),
              'reason_code': reason,
              'status': 'fail',
          }, separators=(',', ':'), sort_keys=True))
          PY
            exit "${replay_status}"
          fi
          python - <<'PY'
          import hashlib, hmac, json, os
          from datetime import datetime, timedelta
          from pathlib import Path
          from top10decision.decision.action_plan import (
              action_plan_semantic_comparison_profile_v3,
              action_plan_semantic_projection_v3,
          )
          from top10decision.decision.canonical_fingerprint import canonical_json_bytes
          from top10decision.decision.model_freeze import (
              load_model_freeze,
              model_freeze_active,
              validate_action_plan_artifact,
          )

          def reject_duplicate_keys(pairs):
              result = {}
              for key, value in pairs:
                  if key in result:
                      raise ValueError('duplicate JSON key')
                  result[key] = value
              return result

          root=Path('.').resolve()
          manifest=load_model_freeze(root, required=True)
          audit=validate_action_plan_artifact(
              root,
              manifest,
              force_enforcement=not model_freeze_active(manifest),
          )
          if audit.get('validated') is not True or audit.get('enforced') is not True:
              raise SystemExit('Daily replayed action plan contract was not enforced')
          raw=(root / 'outputs/decision/action_plan_latest.json').read_bytes()
          digest=hashlib.sha256(raw).hexdigest()
          try:
              action=json.loads(
                  raw.decode('utf-8'),
                  object_pairs_hook=reject_duplicate_keys,
              )
          except (UnicodeError, ValueError):
              raise SystemExit('Daily replayed action JSON is invalid')
          if not isinstance(action, dict):
              raise SystemExit('Daily replayed action must be an object')
          generated_at=action.pop('generated_at_utc', None)
          try:
              generated_time=datetime.fromisoformat(generated_at)
          except (TypeError, ValueError):
              raise SystemExit('Daily replayed action timestamp is invalid')
          if (
              type(generated_at) is not str
              or generated_time.isoformat() != generated_at
              or generated_time.utcoffset() != timedelta(0)
              or generated_time.microsecond != 0
          ):
              raise SystemExit('Daily replayed action timestamp is invalid')
          expected_signal_date=os.environ.get('PERSISTED_ACTION_SIGNAL_DATE', '')
          expected_report_date=os.environ.get('PERSISTED_ACTION_REPORT_DATE', '')
          if os.environ['PUBLISH'] == 'true':
              try:
                  parsed_signal_date=datetime.strptime(expected_signal_date, '%Y%m%d')
              except ValueError:
                  raise SystemExit('Daily persisted Auction action signal_date is invalid')
              if parsed_signal_date.strftime('%Y%m%d') != expected_signal_date:
                  raise SystemExit('Daily persisted Auction action signal_date is invalid')
              if action.get('signal_date') != expected_signal_date:
                  raise SystemExit(
                      'Daily replayed action signal_date differs from persisted Auction action'
                  )
              try:
                  parsed_report_date=datetime.strptime(expected_report_date, '%Y%m%d')
              except ValueError:
                  raise SystemExit('Daily persisted Auction action report_date is invalid')
              if (
                  parsed_report_date.strftime('%Y%m%d') != expected_report_date
                  or parsed_signal_date >= parsed_report_date
                  or action.get('report_date') != expected_report_date
                  or action.get('exec_date') != expected_report_date
              ):
                  raise SystemExit(
                      'Daily replayed action report_date differs from persisted Auction action'
                  )
          elif expected_signal_date or expected_report_date:
              raise SystemExit('Daily dry-run unexpectedly received persisted action dates')
          comparison_profile=os.environ['PERSISTED_ACTION_COMPARISON_PROFILE']
          if os.environ['PUBLISH'] != 'true':
              comparison_profile=action_plan_semantic_comparison_profile_v3(action)
          try:
              semantic_action=action_plan_semantic_projection_v3(
                  action,
                  comparison_profile=comparison_profile,
              )
          except ValueError:
              raise SystemExit('Daily replayed action semantic evidence is invalid')
          semantic_digest=hashlib.sha256(canonical_json_bytes(semantic_action)).hexdigest()
          if os.environ['PUBLISH'] == 'true':
              if os.environ['PERSISTED_ACTION_VALIDATED'] != 'true':
                  raise SystemExit('Daily real publication lacks a validated persisted Auction action')
              print(json.dumps({
                  'comparison_profile': comparison_profile,
                  'component': 'daily_action_semantic_comparison_v3',
                  'persisted_semantic_sha256': os.environ[
                      'PERSISTED_ACTION_SEMANTIC_SHA256'
                  ],
                  'replayed_semantic_sha256': semantic_digest,
                  'status': 'compare',
              }, separators=(',', ':'), sort_keys=True))
              if not hmac.compare_digest(
                  semantic_digest,
                  os.environ['PERSISTED_ACTION_SEMANTIC_SHA256'],
              ):
                  raise SystemExit('Daily replayed action semantics differ from persisted Auction action')
          with open(os.environ['GITHUB_OUTPUT'],'a',encoding='utf-8') as output:
              output.write(f"sha256={digest}\n")
          PY
          auction_reports="${RUNNER_TEMP}/daily-auction-report-paths.bin"
          git ls-files -z --cached -- ':(glob,top)docs/reports/auction_v3*.html' > "${auction_reports}"
          if [ -s "${auction_reports}" ]; then
            git restore --worktree --source=HEAD \
              --pathspec-from-file="${auction_reports}" --pathspec-file-nul
          fi
          git clean -f -- ':(glob,top)docs/reports/auction_v3*.html'
          git status --porcelain=v1 -z --untracked-files=all -- \
            ':(glob,top)docs/reports/auction_v3*.html' \
            > "${RUNNER_TEMP}/daily-auction-report-dirty.bin"
          test ! -s "${RUNNER_TEMP}/daily-auction-report-dirty.bin"
      - name: Resolve Daily exchange write eligibility
        id: session
        env:
          PYTHONPATH: ${{ github.workspace }}/src
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          TRADE_DATE: ${{ inputs.trade_date }}
        run: |
          set -euo pipefail
          args=(--calendar-only)
          if [ -n "${TRADE_DATE}" ]; then args+=(--trade-date "${TRADE_DATE}"); fi
          session_evidence="${RUNNER_TEMP}/daily-session-sync.json"
          python scripts/sync_tushare_minute.py "${args[@]}" > "${session_evidence}"
          SESSION_EVIDENCE="${session_evidence}" python - <<'PY'
          import json, os, re
          from datetime import datetime
          from pathlib import Path
          payload = json.loads(Path(os.environ['SESSION_EVIDENCE']).read_text(encoding='utf-8'))
          if not isinstance(payload, dict): raise SystemExit('Daily session evidence must be one JSON object')
          trade_date = payload.get('trade_date')
          if type(trade_date) is not str or re.fullmatch(r'\d{8}', trade_date) is None:
              raise SystemExit('Daily session evidence trade_date must be exactly 8 digits')
          try:
              parsed_trade_date = datetime.strptime(trade_date, '%Y%m%d').strftime('%Y%m%d')
          except ValueError as exc:
              raise SystemExit('Daily session evidence trade_date is not a valid calendar date') from exc
          if parsed_trade_date != trade_date:
              raise SystemExit('Daily session evidence trade_date is not canonical YYYYMMDD')
          requested_trade_date = os.environ.get('TRADE_DATE', '')
          if requested_trade_date and requested_trade_date != trade_date:
              raise SystemExit('Daily session evidence trade_date differs from requested trade_date')
          status, reason, active = payload.get('status'), payload.get('reason'), payload.get('active_window')
          if type(active) is not bool: raise SystemExit('Daily active_window must be a JSON boolean')
          no_write_reasons = {'exchange_closed', 'outside_active_minute_window'}
          if status == 'not_applicable' and reason in no_write_reasons:
              eligible = False
          elif status == 'success' and reason == 'calendar_synced':
              eligible = True
          else:
              raise SystemExit(f'unexpected Daily session evidence: status={status!r} reason={reason!r}')
          with Path(os.environ['GITHUB_OUTPUT']).open('a', encoding='utf-8') as output:
              output.write(f"write_eligible={'true' if eligible else 'false'}\n")
              output.write(f"target_trade_date={trade_date}\n")
          PY
      - name: Resolve immutable upstream commits before any source write
        id: upstream
        if: ${{ steps.session.outputs.write_eligible == 'true' }}
        env:
          GH_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          python - <<'PY'
          import json, os, re
          from urllib.request import Request, urlopen

          token=os.environ.get('GH_API_TOKEN','').strip()
          if not token: raise SystemExit('GITHUB_TOKEN is required to resolve immutable upstream commits')
          targets=(('pred_sha','njedu2023-prog','a-top10'),('market_sha','njedu2023-prog','a-share-top3-data'))
          output=[]
          for key, owner, repo in targets:
              request=Request(
                  f'https://api.github.com/repos/{owner}/{repo}/commits/main',
                  headers={'Accept':'application/vnd.github+json','Authorization':f'Bearer {token}','X-GitHub-Api-Version':'2022-11-28'},
              )
              with urlopen(request, timeout=30) as response:
                  payload=json.load(response)
              sha=str(payload.get('sha') or '').strip().lower()
              if re.fullmatch(r'[0-9a-f]{40}', sha) is None:
                  raise SystemExit(f'failed to resolve immutable commit for {owner}/{repo}@main')
              output.append(f'{key}={sha}')
          with open(os.environ['GITHUB_OUTPUT'],'a',encoding='utf-8') as handle:
              handle.write('\n'.join(output)+'\n')
          PY
      - name: Sync prediction and market source snapshots
        if: ${{ steps.session.outputs.write_eligible == 'true' }}
        env:
          PYTHONPATH: ${{ github.workspace }}/src
          TRADE_DATE: ${{ steps.session.outputs.target_trade_date }}
          PUBLISH: ${{ steps.mode.outputs.publish }}
          MARKET_RAW_OWNER: njedu2023-prog
          MARKET_RAW_REPO: a-share-top3-data
          MARKET_RAW_BRANCH: main
          MARKET_RAW_COMMIT: ${{ steps.upstream.outputs.market_sha }}
          TOP10_PRED_RESOLVED_COMMIT: ${{ steps.upstream.outputs.pred_sha }}
          A_TOP10_COMMIT: ${{ steps.upstream.outputs.pred_sha }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p data/pred/archive data/market/raw data/decision docs/signals docs/reports docs/weights outputs/decision outputs/learning models
          [[ "${TRADE_DATE}" =~ ^[0-9]{8}$ ]]
          url="https://raw.githubusercontent.com/njedu2023-prog/a-top10/${A_TOP10_COMMIT}/outputs/decisio/pred_decisio_${TRADE_DATE}.csv"
          TRADE_DATE="${TRADE_DATE}" TOP10_PRED_URL="${url}" python scripts/sync_pred_source.py
          python scripts/sync_market_raw.py \
            --trade-date "${TRADE_DATE}" \
            --ensure-sse-open-context 21 \
            --strict-dated-source \
            --trade-calendar-file data/market/trade_cal_sse.csv
      - name: Build market features and strict exchange calendar
        if: ${{ steps.session.outputs.write_eligible == 'true' }}
        env:
          PYTHONPATH: ${{ github.workspace }}/src
          TRADE_DATE: ${{ steps.session.outputs.target_trade_date }}
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        run: |
          set -euo pipefail
          [[ "${TRADE_DATE}" =~ ^[0-9]{8}$ ]]
          python scripts/build_market_fs.py --trade-date "${TRADE_DATE}"
      - name: Pin the last Auction-validated action plan
        id: preserved_action
        if: ${{ steps.session.outputs.write_eligible == 'true' }}
        env:
          FROZEN_ACTION_SHA256: ${{ steps.frozen_action.outputs.sha256 }}
          PYTHONPATH: ${{ github.workspace }}/src
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib, hmac, os
          from pathlib import Path
          from top10decision.decision.model_freeze import (
              load_model_freeze,
              model_freeze_active,
              validate_action_plan_artifact,
          )

          root=Path('.').resolve()
          manifest=load_model_freeze(root, required=True)
          audit=validate_action_plan_artifact(
              root,
              manifest,
              force_enforcement=not model_freeze_active(manifest),
          )
          if audit.get('validated') is not True or audit.get('enforced') is not True:
              raise SystemExit('Daily action plan contract was not enforced')
          path=root / 'outputs/decision/action_plan_latest.json'
          raw=path.read_bytes()
          digest=hashlib.sha256(raw).hexdigest()
          if not hmac.compare_digest(digest, os.environ['FROZEN_ACTION_SHA256']):
              raise SystemExit('Daily source synchronization modified the frozen Auction action plan')
          with open(os.environ['GITHUB_OUTPUT'],'a',encoding='utf-8') as output:
              output.write(f"sha256={digest}\n")
          PY
      - name: Run Daily Decision once with learning and refresh disabled
        if: ${{ steps.session.outputs.write_eligible == 'true' }}
        env:
          PYTHONPATH: ${{ github.workspace }}/src
          TRADE_DATE: ${{ steps.session.outputs.target_trade_date }}
          PUBLISH: ${{ steps.mode.outputs.publish }}
          PRESERVED_ACTION_SHA256: ${{ steps.preserved_action.outputs.sha256 }}
          DECISION_RUN_RECEIPT_PATH: ${{ runner.temp }}/decision-run-receipt.json
        run: |
          set -euo pipefail
          [[ "${TRADE_DATE}" =~ ^[0-9]{8}$ ]]
          python scripts/run_v2.py --trade-date "${TRADE_DATE}"
          python - <<'PY'
          import hashlib, hmac, os
          from pathlib import Path

          path=Path('outputs/decision/action_plan_latest.json')
          actual=hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else ''
          if not hmac.compare_digest(actual, os.environ['PRESERVED_ACTION_SHA256']):
              raise SystemExit('Daily modified the preserved Auction action plan')
          PY
          python scripts/validate_io_contract.py --strict-semantic
          if [ "${PUBLISH}" = "true" ]; then
            python scripts/validate_decision_model_freeze.py --runtime
          else
            python scripts/validate_decision_model_freeze.py --runtime --force-inactive
          fi
      - name: Build isolated full Daily research context without creating action
        if: ${{ steps.session.outputs.write_eligible == 'true' }}
        env:
          PUBLISH: ${{ steps.mode.outputs.publish }}
          PRESERVED_ACTION_SHA256: ${{ steps.preserved_action.outputs.sha256 }}
          TRADE_DATE: ${{ steps.session.outputs.target_trade_date }}
          DECISION_RUN_RECEIPT_PATH: ${{ runner.temp }}/decision-run-receipt.json
          PYTHONPATH: ${{ github.workspace }}/src
          DC20_PERSIST_LEGACY_PROFIT_PRIVATE_RUNTIME: "1"
        run: |
          set -euo pipefail
          research_root="${RUNNER_TEMP}/dc20-daily-research-root"
          mkdir -p "${research_root}"
          rsync -a --delete --exclude='.git' --exclude='__pycache__' \
            --exclude='.dc20-private' ./ "${research_root}/"
          read -r signal_date report_date < <(python - <<'PY'
          import os
          from pathlib import Path

          from top10decision.decision.run_receipt import validate_decision_run_receipt

          receipt = validate_decision_run_receipt(
              root=Path.cwd(),
              receipt_path=Path(os.environ['DECISION_RUN_RECEIPT_PATH']),
              requested_trade_date=os.environ.get('TRADE_DATE', ''),
          )
          print(receipt['signal_date'], receipt['exec_date'])
          PY
          )
          auction_args=(--root "${research_root}" --signal-date "${signal_date}")
          if [ "${PUBLISH}" != "true" ]; then auction_args+=(--force-inactive); fi
          python "${research_root}/scripts/run_deterministic_numeric.py" \
            "${research_root}/scripts/run_auction_v3.py" "${auction_args[@]}"
          python "${research_root}/scripts/publish_decision_research_context.py" \
            --source-root "${research_root}" \
            --output-root "${research_root}" \
            --report-date "${report_date}"
          research_path="${research_root}/outputs/decision/research_context_dc20_${report_date}.json"
          if [ ! -f "${research_path}" ]; then
            research_path="${research_root}/outputs/decision/research_context_${report_date}.json"
          fi
          RESEARCH_PATH="${research_path}" \
            RESEARCH_ROOT="${research_root}" \
            REPORT_DATE="${report_date}" \
            PYTHONPATH="${GITHUB_WORKSPACE}/src" \
            python - <<'PY'
          import hashlib
          import hmac
          import json
          import os
          from pathlib import Path

          from top10decision.decision.research_context import (
              HISTORICAL_PARITY_SCHEMA,
              validate_historical_parity_context,
              validate_research_context,
          )

          context = json.loads(Path(os.environ['RESEARCH_PATH']).read_text(encoding='utf-8'))
          if context.get('schema_version') == HISTORICAL_PARITY_SCHEMA:
              validate_historical_parity_context(
                  context,
                  expected_report_date=os.environ['REPORT_DATE'],
              )
              print('Daily preserved historical parity research context')
          else:
              validate_research_context(context, expected_report_date=os.environ['REPORT_DATE'])
          for label, action in (
              ('workspace', Path('outputs/decision/action_plan_latest.json')),
              (
                  'isolated research root',
                  Path(os.environ['RESEARCH_ROOT'])
                  / 'outputs/decision/action_plan_latest.json',
              ),
          ):
              digest = (
                  hashlib.sha256(action.read_bytes()).hexdigest()
                  if action.is_file()
                  else ''
              )
              if not hmac.compare_digest(
                  digest, os.environ['PRESERVED_ACTION_SHA256']
              ):
                  raise SystemExit(
                      f'Daily research generation modified the preserved '
                      f'Auction action plan in {label}'
                  )
          PY
      - name: Freeze immutable executable-profit research order and public D projection
        if: ${{ steps.session.outputs.write_eligible == 'true' }}
        env:
          DECISION_RUN_RECEIPT_PATH: ${{ runner.temp }}/decision-run-receipt.json
          PRESERVED_ACTION_SHA256: ${{ steps.preserved_action.outputs.sha256 }}
          TRADE_DATE: ${{ steps.session.outputs.target_trade_date }}
          PYTHONPATH: ${{ github.workspace }}/src
        run: |
          set -euo pipefail
          research_root="${RUNNER_TEMP}/dc20-daily-research-root"
          selection_inventory="${RUNNER_TEMP}/executable-profit-selection-before.json"
          SELECTION_INVENTORY="${selection_inventory}" python - <<'PY'
          import hashlib
          import hmac
          import json
          import os
          from pathlib import Path

          root = Path('.').resolve()
          selection_root = root / 'data/decision_executable_profit/forward/selections'
          inventory = {}
          if selection_root.exists():
              if not selection_root.is_dir() or selection_root.is_symlink():
                  raise SystemExit('Daily executable-profit selection root is unsafe')
              for path in sorted(selection_root.glob('shadow_20??????.*')):
                  if path.suffix not in {'.json', '.csv'}:
                      continue
                  if not path.is_file() or path.is_symlink():
                      raise SystemExit(f'Daily executable-profit selection is unsafe: {path}')
                  inventory[path.relative_to(root).as_posix()] = hashlib.sha256(
                      path.read_bytes()
                  ).hexdigest()
          Path(os.environ['SELECTION_INVENTORY']).write_text(
              json.dumps(inventory, sort_keys=True, separators=(',', ':')) + '\n',
              encoding='utf-8',
          )
          PY
          read -r signal_date report_date < <(python - <<'PY'
          import os
          from pathlib import Path

          from top10decision.decision.run_receipt import validate_decision_run_receipt

          receipt = validate_decision_run_receipt(
              root=Path.cwd(),
              receipt_path=Path(os.environ['DECISION_RUN_RECEIPT_PATH']),
              requested_trade_date=os.environ.get('TRADE_DATE', ''),
          )
          print(receipt['signal_date'], receipt['exec_date'])
          PY
          )
          printf '%s\n' "${signal_date}" > "${RUNNER_TEMP}/daily-signal-date.txt"
          feature_path="${research_root}/outputs/auction_v3/predictions/pred_${signal_date}.csv"
          require_regular_nonempty() {
            local path="$1"
            local label="$2"
            if [ ! -f "${path}" ] || [ -L "${path}" ] || [ ! -s "${path}" ]; then
              echo "Daily isolated ${label} is missing, empty, or unsafe: ${path}" >&2
              exit 1
            fi
          }
          require_regular_nonempty "${feature_path}" "canonical pred_D feature"
          require_regular_nonempty \
            "${research_root}/outputs/decision/three_rank_top10_${signal_date}.json" \
            "three-rank JSON for signal_date=${signal_date}"
          require_regular_nonempty \
            "${research_root}/outputs/decision/three_rank_top10_${signal_date}.csv" \
            "three-rank CSV for signal_date=${signal_date}"
          require_regular_nonempty \
            "${research_root}/outputs/decision/three_rank_index.json" \
            "three-rank index"
          require_regular_nonempty \
            "${research_root}/outputs/decision/research_context_dc20_${report_date}.json" \
            "DC20 research context for report_date=${report_date}"
          root_contract_evidence="${RUNNER_TEMP}/executable-profit-root-contract.json"
          RESEARCH_ROOT="${research_root}" \
            FEATURE_PATH="${feature_path}" \
            SIGNAL_DATE="${signal_date}" \
            ROOT_CONTRACT_EVIDENCE="${root_contract_evidence}" \
            python - <<'PY'
          import json
          import os
          import re
          from pathlib import Path

          signal_date = os.environ['SIGNAL_DATE']
          if re.fullmatch(r'20\d{6}', signal_date) is None:
              raise SystemExit('Daily executable-profit signal date is invalid')
          research_root = Path(os.environ['RESEARCH_ROOT']).resolve(strict=True)
          feature_path = Path(os.environ['FEATURE_PATH']).resolve(strict=True)
          expected = (
              research_root
              / 'outputs/auction_v3/predictions'
              / f'pred_{signal_date}.csv'
          )
          if (
              expected.is_symlink()
              or not expected.is_file()
              or expected.stat().st_size <= 0
              or feature_path != expected.resolve(strict=True)
          ):
              raise SystemExit(
                  'Daily scorer root and canonical pred_D feature root differ'
              )
          evidence = {
              'schema_version': 'dc20_executable_profit_scorer_root_binding_v1',
              'repo_root': research_root.as_posix(),
              'd_feature_path': feature_path.as_posix(),
              'canonical_relative_path': (
                  f'outputs/auction_v3/predictions/pred_{signal_date}.csv'
              ),
              'same_repository_root': True,
              'signal_date': signal_date,
          }
          Path(os.environ['ROOT_CONTRACT_EVIDENCE']).write_text(
              json.dumps(evidence, sort_keys=True, separators=(',', ':')) + '\n',
              encoding='utf-8',
          )
          PY
          PYTHONPATH="${research_root}/src" python "${research_root}/scripts/run_decision_executable_profit_forward_shadow.py" \
            --root "${research_root}" \
            --d-features-csv "${feature_path}"
          PYTHONPATH="${research_root}/src" python "${research_root}/scripts/settle_decision_executable_profit_forward_shadow.py" \
            --root "${research_root}" \
            --as-of-date "${signal_date}" \
            --statistics-only
          PYTHONPATH="${research_root}/src" python "${research_root}/scripts/project_decision_executable_profit_research.py" \
            --root "${research_root}" \
            --signal-date "${signal_date}" \
            --as-of-date "${signal_date}"
          SOURCE_ROOT="${research_root}" \
            TARGET_ROOT="${GITHUB_WORKSPACE}" \
            SIGNAL_DATE="${signal_date}" \
            REPORT_DATE="${report_date}" \
            python - <<'PY'
          import hashlib
          import hmac
          import json
          import os
          import tempfile
          from pathlib import Path

          import pandas as pd

          from top10decision.decision.executable_profit_research_projection import (
              _validate_existing_index_chain as validate_public_index_chain,
              validate_research_projection,
              validate_research_projection_index,
              validate_shadow_statistics_projection,
          )
          from top10decision.decision.executable_profit_shadow import (
              _validate_existing_pointer_chain as validate_selection_index_chain,
              validate_internal_forward_shadow_index,
              validate_internal_forward_shadow_payload,
          )
          from top10decision.decision.executable_profit_shadow_settlement import (
              validate_statistics,
          )
          from top10decision.decision.research_context import (
              validate_research_context,
          )
          from top10decision.decision.three_rank import (
              _three_engine_projection_is_complete,
              _three_rank_index_payload,
              validate_three_rank_contract,
              validate_three_rank_index,
          )

          source_root = Path(os.environ['SOURCE_ROOT']).resolve(strict=True)
          target_root = Path(os.environ['TARGET_ROOT']).resolve(strict=True)
          signal_date = os.environ['SIGNAL_DATE']
          report_date = os.environ['REPORT_DATE']
          if source_root == target_root:
              raise SystemExit('Daily research projection source must be isolated')

          decision_prefix = Path('outputs/decision')
          three_rank_json_relative = (
              decision_prefix / f'three_rank_top10_{signal_date}.json'
          )
          three_rank_csv_relative = (
              decision_prefix / f'three_rank_top10_{signal_date}.csv'
          )
          three_rank_index_relative = decision_prefix / 'three_rank_index.json'
          research_context_relative = (
              decision_prefix / f'research_context_dc20_{report_date}.json'
          )
          canonical_prediction_relative = (
              Path('outputs/auction_v3/predictions')
              / f'pred_{signal_date}.csv'
          )
          selection_prefix = Path(
              'data/decision_executable_profit/forward/selections'
          )
          statistics_relative = Path(
              'data/decision_executable_profit/forward/statistics/summary.json'
          )
          public_prefix = Path('outputs/decision/executable_profit_research')
          selection_json_relative = selection_prefix / f'shadow_{signal_date}.json'
          selection_csv_relative = selection_prefix / f'shadow_{signal_date}.csv'
          selection_index_relative = selection_prefix / 'index.json'
          projection_json_relative = public_prefix / f'projection_{signal_date}.json'
          projection_csv_relative = public_prefix / f'projection_{signal_date}.csv'
          public_index_relative = public_prefix / 'index.json'

          def safe_source(relative: Path) -> Path:
              if relative.is_absolute() or '..' in relative.parts:
                  raise SystemExit(f'Daily research artifact path is unsafe: {relative}')
              current = source_root
              for part in relative.parts:
                  current = current / part
                  if current.is_symlink():
                      raise SystemExit(
                          f'Daily research artifact has a symlink ancestor: {relative}'
                      )
              if not current.is_file():
                  raise SystemExit(f'Daily research artifact is missing: {relative}')
              if current.resolve(strict=True) != current:
                  raise SystemExit(f'Daily research artifact escaped source root: {relative}')
              return current

          selection_json = safe_source(selection_json_relative)
          selection_csv = safe_source(selection_csv_relative)
          selection_index_path = safe_source(selection_index_relative)
          summary_path = safe_source(statistics_relative)
          projection_json = safe_source(projection_json_relative)
          projection_csv = safe_source(projection_csv_relative)
          public_index_path = safe_source(public_index_relative)
          three_rank_json = safe_source(three_rank_json_relative)
          three_rank_csv = safe_source(three_rank_csv_relative)
          three_rank_index_path = safe_source(three_rank_index_relative)
          research_context_path = safe_source(research_context_relative)
          canonical_prediction_path = safe_source(
              canonical_prediction_relative
          )

          three_rank = json.loads(three_rank_json.read_text(encoding='utf-8'))
          validate_three_rank_contract(three_rank)
          three_rank_index = json.loads(
              three_rank_index_path.read_text(encoding='utf-8')
          )
          validate_three_rank_index(three_rank_index)
          research_context = json.loads(
              research_context_path.read_text(encoding='utf-8')
          )
          validate_research_context(
              research_context,
              expected_report_date=report_date,
          )
          three_rank_json_sha256 = hashlib.sha256(
              three_rank_json.read_bytes()
          ).hexdigest()
          three_rank_csv_sha256 = hashlib.sha256(
              three_rank_csv.read_bytes()
          ).hexdigest()
          expected_three_rank_index = _three_rank_index_payload(
              three_rank,
              contract_sha256=three_rank_json_sha256,
          )
          expected_context_files = {
              three_rank['downloads']['json_url']: three_rank_json_sha256,
              three_rank['downloads']['csv_url']: three_rank_csv_sha256,
          }
          if (
              three_rank.get('signal_date') != signal_date
              or three_rank_index != expected_three_rank_index
              or research_context.get('signal_date') != signal_date
              or research_context.get('report_date') != report_date
              or research_context.get('three_rank') != three_rank
              or (research_context.get('source_binding') or {}).get('files')
              != expected_context_files
          ):
              raise SystemExit(
                  'Daily isolated three-rank/context binding drifted'
              )

          canonical_prediction = pd.read_csv(canonical_prediction_path)
          if not _three_engine_projection_is_complete(
              canonical_prediction,
              signal_date,
          ):
              raise SystemExit(
                  'Daily canonical pred_D three-engine projection is incomplete'
              )
          if canonical_prediction.empty:
              raise SystemExit(
                  'Daily canonical pred_D hard-range inference pool is empty'
              )
          if not canonical_prediction.empty:
              expected_dates = {
                  'signal_date': signal_date,
                  'expected_buy_date': str(three_rank['exec_date']),
                  'expected_exit_date': str(three_rank['exit_date']),
              }
              for column, expected in expected_dates.items():
                  values = {
                      str(value).strip().removesuffix('.0')
                      for value in canonical_prediction[column].tolist()
                  }
                  if values != {expected}:
                      raise SystemExit(
                          f'Daily canonical pred_D {column} binding drifted'
                      )
          selected_prediction = canonical_prediction.loc[
              pd.to_numeric(
                  canonical_prediction['top10_selected'], errors='coerce'
              ).eq(1)
          ].copy()
          selected_prediction['_promotion_rank'] = pd.to_numeric(
              selected_prediction['promotion_rank'], errors='coerce'
          )
          selected_prediction = selected_prediction.sort_values(
              '_promotion_rank', kind='stable'
          )
          prediction_identity = [
              (
                  str(row['ts_code']),
                  int(row['_promotion_rank']),
                  str(row['stage']).replace('->', '→'),
                  round(float(row['predicted_promotion_probability']), 10),
              )
              for _, row in selected_prediction.iterrows()
          ]
          contract_identity = [
              (
                  str(row['ts_code']),
                  int(row['promotion_rank']),
                  str(row['stage_transition']).replace('->', '→'),
                  round(float(row['predicted_promotion_probability']), 10),
              )
              for row in sorted(
                  three_rank['rows'], key=lambda row: int(row['promotion_rank'])
              )
          ]
          if prediction_identity != contract_identity:
              raise SystemExit(
                  'Daily canonical pred_D promotion membership or order drifted'
              )
          if not canonical_prediction.empty and (
              set(
                  canonical_prediction['top10_members_sha256']
                  .fillna('')
                  .astype(str)
              )
              != {str(three_rank['top10_members_sha256'])}
              or set(
                  canonical_prediction['feature_snapshot_sha256']
                  .fillna('')
                  .astype(str)
              )
              != {str(three_rank['feature_snapshot_sha256'])}
          ):
              raise SystemExit(
                  'Daily canonical pred_D hash binding drifted'
              )
          for column in (
              'selected',
              'first_layer_selected',
              'trade_selected',
          ):
              values = pd.to_numeric(
                  canonical_prediction[column], errors='coerce'
              )
              if values.isna().any() or not values.eq(0).all():
                  raise SystemExit(
                      f'Daily canonical pred_D contains formal action: {column}'
                  )
          actions = set(
              canonical_prediction['action'].fillna('').astype(str).str.strip()
          )
          if not actions.issubset({'REJECT', 'SHADOW_ONLY'}):
              raise SystemExit(
                  'Daily canonical pred_D contains a non-shadow action'
              )
          for column, expected in (
              ('guidance_only', 1),
              ('broker_connected', 0),
              ('market_order_allowed', 0),
          ):
              values = pd.to_numeric(
                  canonical_prediction[column], errors='coerce'
              )
              if values.isna().any() or not values.eq(expected).all():
                  raise SystemExit(
                      f'Daily canonical pred_D action boundary drifted: {column}'
                  )
          order_types = set(
              canonical_prediction['order_type']
              .fillna('')
              .astype(str)
              .str.strip()
          )
          if not order_types.issubset({'LIMIT_ONLY_MANUAL'}):
              raise SystemExit(
                  'Daily canonical pred_D order_type boundary drifted'
              )

          source_action = (
              source_root / 'outputs/decision/action_plan_latest.json'
          )
          source_action_sha256 = (
              hashlib.sha256(source_action.read_bytes()).hexdigest()
              if source_action.is_file() and not source_action.is_symlink()
              else ''
          )
          if not hmac.compare_digest(
              source_action_sha256,
              os.environ['PRESERVED_ACTION_SHA256'],
          ):
              raise SystemExit(
                  'Daily executable-profit scorer modified the isolated '
                  'preserved action'
              )

          selection = json.loads(selection_json.read_text(encoding='utf-8'))
          validate_internal_forward_shadow_payload(selection, require_downloads=True)
          selection_index = json.loads(
              selection_index_path.read_text(encoding='utf-8')
          )
          validate_internal_forward_shadow_index(selection_index)
          validate_selection_index_chain(source_root, selection_index)
          summary = json.loads(summary_path.read_text(encoding='utf-8'))
          validate_statistics(summary)
          projection = json.loads(projection_json.read_text(encoding='utf-8'))
          validate_research_projection(projection, require_downloads=True)
          canonical_prediction_sha256 = hashlib.sha256(
              canonical_prediction_path.read_bytes()
          ).hexdigest()
          selection_d_feature = selection.get('source_d_feature') or {}
          projection_selection_binding = (
              (projection.get('source_bindings') or {}).get('selection') or {}
          )
          if (
              selection_d_feature.get('file_name')
              != canonical_prediction_relative.name
              or selection_d_feature.get('file_sha256')
              != canonical_prediction_sha256
              or selection_d_feature.get('selected_row_count')
              != len(contract_identity)
              or projection_selection_binding.get('d_feature_file_sha256')
              != canonical_prediction_sha256
          ):
              raise SystemExit(
                  'Daily canonical pred_D selection/projection SHA binding drifted'
              )
          public_index = json.loads(public_index_path.read_text(encoding='utf-8'))
          validate_research_projection_index(public_index)
          validate_public_index_chain(source_root, public_index)
          public_statistics_relative = Path(str(public_index['latest_statistics_url']))
          expected_public_statistics = (
              public_prefix
              / f'shadow_statistics_{signal_date}_asof_{signal_date}.json'
          )
          if public_statistics_relative != expected_public_statistics:
              raise SystemExit('Daily public statistics D/as-of path drifted')
          public_statistics_path = safe_source(public_statistics_relative)
          public_statistics = json.loads(
              public_statistics_path.read_text(encoding='utf-8')
          )
          validate_shadow_statistics_projection(public_statistics)
          if (
              selection.get('signal_date') != signal_date
              or selection_index.get('latest_signal_date') != signal_date
              or summary.get('as_of_date') != signal_date
              or projection.get('signal_date') != signal_date
              or public_index.get('latest_signal_date') != signal_date
              or public_index.get('latest_statistics_as_of_date') != signal_date
              or public_statistics.get('signal_date') != signal_date
              or public_statistics.get('as_of_date') != signal_date
          ):
              raise SystemExit('Daily isolated research artifacts lost exact D binding')

          immutable_relatives = (
              canonical_prediction_relative,
              three_rank_json_relative,
              three_rank_csv_relative,
              research_context_relative,
              selection_json_relative,
              selection_csv_relative,
              projection_json_relative,
              projection_csv_relative,
              public_statistics_relative,
          )
          mutable_relatives = (
              three_rank_index_relative,
              statistics_relative,
              selection_index_relative,
              public_index_relative,
          )
          exact_relatives = immutable_relatives + mutable_relatives
          if len(set(exact_relatives)) != 13:
              raise SystemExit('Daily research projection copy inventory drifted')

          def target_path(relative: Path) -> Path:
              current = target_root
              for part in relative.parent.parts:
                  current = current / part
                  if current.exists():
                      if not current.is_dir() or current.is_symlink():
                          raise SystemExit(
                              f'Daily target artifact has an unsafe ancestor: {relative}'
                          )
                  else:
                      current.mkdir()
              path = target_root / relative
              if path.exists() and (not path.is_file() or path.is_symlink()):
                  raise SystemExit(f'Daily target artifact is unsafe: {relative}')
              return path

          def install_immutable(relative: Path) -> None:
              source = safe_source(relative)
              payload = source.read_bytes()
              target = target_path(relative)
              if target.exists():
                  if target.read_bytes() != payload:
                      raise SystemExit(
                          f'Daily immutable research artifact conflict: {relative}'
                      )
                  return
              with tempfile.NamedTemporaryFile(
                  'wb', dir=target.parent, prefix=f'.{target.name}.', delete=False
              ) as handle:
                  temporary = Path(handle.name)
                  handle.write(payload)
                  handle.flush()
                  os.fsync(handle.fileno())
              try:
                  os.link(temporary, target)
              except FileExistsError:
                  if target.read_bytes() != payload:
                      raise SystemExit(
                          f'Daily immutable research artifact raced: {relative}'
                      )
              finally:
                  temporary.unlink(missing_ok=True)

          def replace_pointer(relative: Path) -> None:
              payload = safe_source(relative).read_bytes()
              target = target_path(relative)
              with tempfile.NamedTemporaryFile(
                  'wb', dir=target.parent, prefix=f'.{target.name}.', delete=False
              ) as handle:
                  temporary = Path(handle.name)
                  handle.write(payload)
                  handle.flush()
                  os.fsync(handle.fileno())
              try:
                  os.replace(temporary, target)
              finally:
                  temporary.unlink(missing_ok=True)

          for relative in immutable_relatives:
              install_immutable(relative)
          for relative in mutable_relatives:
              replace_pointer(relative)
          print(json.dumps({
              'status': 'EXACT_RESEARCH_PROJECTION_COPIED',
              'signal_date': signal_date,
              'source_root': source_root.as_posix(),
              'target_root': target_root.as_posix(),
              'copied_paths': [value.as_posix() for value in exact_relatives],
              'outputs_auction_v3_copied': True,
              'canonical_prediction_only': True,
              'isolated_three_rank_and_context_copied': True,
          }, ensure_ascii=False, sort_keys=True))
          PY
          SIGNAL_DATE="${signal_date}" \
            REPORT_DATE="${report_date}" \
            SELECTION_INVENTORY="${selection_inventory}" \
            python - <<'PY'
          import hashlib
          import hmac
          import json
          import os
          from pathlib import Path

          from top10decision.decision.executable_profit_research_projection import (
              _validate_existing_index_chain as validate_public_index_chain,
              validate_research_projection,
              validate_research_projection_index,
              validate_shadow_statistics_projection,
          )
          from top10decision.decision.executable_profit_shadow import (
              _validate_existing_pointer_chain as validate_selection_index_chain,
              validate_internal_forward_shadow_index,
              validate_internal_forward_shadow_payload,
          )
          from top10decision.decision.executable_profit_shadow_settlement import (
              validate_statistics,
          )
          from top10decision.decision.research_context import (
              validate_research_context,
          )
          from top10decision.decision.three_rank import (
              _three_rank_index_payload,
              validate_three_rank_contract,
              validate_three_rank_index,
          )

          root = Path('.').resolve()
          signal_date = os.environ['SIGNAL_DATE']
          report_date = os.environ['REPORT_DATE']
          selection_root = root / 'data/decision_executable_profit/forward/selections'
          before = json.loads(
              Path(os.environ['SELECTION_INVENTORY']).read_text(encoding='utf-8')
          )
          after = {}
          for path in sorted(selection_root.glob('shadow_20??????.*')):
              if path.suffix not in {'.json', '.csv'}:
                  continue
              if not path.is_file() or path.is_symlink():
                  raise SystemExit(f'Daily executable-profit selection is unsafe: {path}')
              after[path.relative_to(root).as_posix()] = hashlib.sha256(
                  path.read_bytes()
              ).hexdigest()
          for relative, digest in before.items():
              if after.get(relative) != digest:
                  raise SystemExit(f'Daily rewrote an immutable selection: {relative}')
          additions = set(after) - set(before)
          exact_additions = {
              f'data/decision_executable_profit/forward/selections/shadow_{signal_date}.json',
              f'data/decision_executable_profit/forward/selections/shadow_{signal_date}.csv',
          }
          if not additions.issubset(exact_additions):
              raise SystemExit(f'Daily created a foreign selection artifact: {sorted(additions)}')

          selection_path = selection_root / f'shadow_{signal_date}.json'
          selection = json.loads(selection_path.read_text(encoding='utf-8'))
          validate_internal_forward_shadow_payload(selection, require_downloads=True)
          selection_index = json.loads(
              (selection_root / 'index.json').read_text(encoding='utf-8')
          )
          validate_internal_forward_shadow_index(selection_index)
          validate_selection_index_chain(root, selection_index)
          if selection_index.get('latest_signal_date') != signal_date:
              raise SystemExit('Daily executable-profit selection pointer date drifted')
          summary = json.loads(
              (
                  root
                  / 'data/decision_executable_profit/forward/statistics/summary.json'
              ).read_text(encoding='utf-8')
          )
          validate_statistics(summary)
          if summary.get('as_of_date') != signal_date:
              raise SystemExit('Daily executable-profit summary as-of date drifted')

          public_root = root / 'outputs/decision/executable_profit_research'
          projection = json.loads(
              (public_root / f'projection_{signal_date}.json').read_text(encoding='utf-8')
          )
          validate_research_projection(projection, require_downloads=True)
          public_index = json.loads((public_root / 'index.json').read_text(encoding='utf-8'))
          validate_research_projection_index(public_index)
          validate_public_index_chain(root, public_index)
          statistics_path = root / str(public_index['latest_statistics_url'])
          statistics = json.loads(statistics_path.read_text(encoding='utf-8'))
          validate_shadow_statistics_projection(statistics)
          if (
              projection.get('signal_date') != signal_date
              or public_index.get('latest_signal_date') != signal_date
              or public_index.get('latest_statistics_as_of_date') != signal_date
              or projection.get('candidate_count') != len(selection.get('rows') or [])
              or projection.get('candidate_count') < 0
              or projection.get('candidate_count') > 10
          ):
              raise SystemExit('Daily executable-profit projection/date/count binding drifted')

          three_rank_path = (
              root / f'outputs/decision/three_rank_top10_{signal_date}.json'
          )
          three_rank_csv_path = (
              root / f'outputs/decision/three_rank_top10_{signal_date}.csv'
          )
          three_rank = json.loads(three_rank_path.read_text(encoding='utf-8'))
          validate_three_rank_contract(three_rank)
          three_rank_index = json.loads(
              (root / 'outputs' / 'decision' / 'three_rank_index.json').read_text(
                  encoding='utf-8'
              )
          )
          validate_three_rank_index(three_rank_index)
          research_context = json.loads(
              (
                  root
                  / f'outputs/decision/research_context_dc20_{report_date}.json'
              ).read_text(encoding='utf-8')
          )
          validate_research_context(
              research_context,
              expected_report_date=report_date,
          )
          three_rank_json_sha256 = hashlib.sha256(
              three_rank_path.read_bytes()
          ).hexdigest()
          three_rank_csv_sha256 = hashlib.sha256(
              three_rank_csv_path.read_bytes()
          ).hexdigest()
          expected_three_rank_index = _three_rank_index_payload(
              three_rank,
              contract_sha256=three_rank_json_sha256,
          )
          expected_context_files = {
              three_rank['downloads']['json_url']: three_rank_json_sha256,
              three_rank['downloads']['csv_url']: three_rank_csv_sha256,
          }
          if (
              three_rank.get('signal_date') != signal_date
              or three_rank_index != expected_three_rank_index
              or research_context.get('signal_date') != signal_date
              or research_context.get('report_date') != report_date
              or research_context.get('three_rank') != three_rank
              or (research_context.get('source_binding') or {}).get('files')
              != expected_context_files
          ):
              raise SystemExit(
                  'Daily copied three-rank/context binding drifted'
              )

          action = root / 'outputs/decision/action_plan_latest.json'
          action_digest = hashlib.sha256(action.read_bytes()).hexdigest() if action.is_file() else ''
          if not hmac.compare_digest(action_digest, os.environ['PRESERVED_ACTION_SHA256']):
              raise SystemExit('Daily executable-profit projection modified the preserved action')
          PY
      - name: Project isolated legacy-profit relative research sidecar
        if: ${{ steps.session.outputs.write_eligible == 'true' }}
        env:
          DECISION_RUN_RECEIPT_PATH: ${{ runner.temp }}/decision-run-receipt.json
          PRESERVED_ACTION_SHA256: ${{ steps.preserved_action.outputs.sha256 }}
          TRADE_DATE: ${{ steps.session.outputs.target_trade_date }}
          PYTHONPATH: ${{ github.workspace }}/src
        run: |
          set -euo pipefail
          research_root="${RUNNER_TEMP}/dc20-daily-research-root"
          read -r signal_date report_date < <(python - <<'PY'
          import os
          from pathlib import Path

          from top10decision.decision.run_receipt import validate_decision_run_receipt

          receipt = validate_decision_run_receipt(
              root=Path.cwd(),
              receipt_path=Path(os.environ['DECISION_RUN_RECEIPT_PATH']),
              requested_trade_date=os.environ.get('TRADE_DATE', ''),
          )
          print(receipt['signal_date'], receipt['exec_date'])
          PY
          )
          private_relative=".dc20-private/legacy_profit_relative_research/runtime_features_${signal_date}.csv"
          private_features="${research_root}/${private_relative}"
          canonical_relative="data/decision/legacy_profit_relative/runtime_features_${signal_date}.csv"
          canonical_features="${research_root}/${canonical_relative}"
          test -f "${private_features}"
          test ! -L "${private_features}"
          test -s "${private_features}"
          RESEARCH_ROOT="${research_root}" \
            PRIVATE_FEATURES="${private_features}" \
            CANONICAL_FEATURES="${canonical_features}" \
            SIGNAL_DATE="${signal_date}" \
            python - <<'PY'
          import os
          import re
          import tempfile
          from pathlib import Path

          root = Path(os.environ['RESEARCH_ROOT']).resolve(strict=True)
          signal_date = os.environ['SIGNAL_DATE']
          if re.fullmatch(r'20\d{6}', signal_date) is None:
              raise SystemExit('Legacy-profit runtime feature date is invalid')
          source = Path(os.environ['PRIVATE_FEATURES']).resolve(strict=True)
          expected_source = (
              root
              / '.dc20-private/legacy_profit_relative_research'
              / f'runtime_features_{signal_date}.csv'
          )
          target = Path(os.environ['CANONICAL_FEATURES'])
          expected_target = (
              root
              / 'data/decision/legacy_profit_relative'
              / f'runtime_features_{signal_date}.csv'
          )
          if (
              source != expected_source
              or expected_source.is_symlink()
              or target != expected_target
              or target.is_symlink()
          ):
              raise SystemExit('Legacy-profit private/canonical feature path drifted')
          payload = source.read_bytes()
          if not payload:
              raise SystemExit('Legacy-profit private feature input is empty')
          target.parent.mkdir(parents=True, exist_ok=True)
          if target.exists():
              if not target.is_file() or target.is_symlink() or target.read_bytes() != payload:
                  raise SystemExit('Legacy-profit immutable runtime evidence conflict')
          else:
              with tempfile.NamedTemporaryFile(
                  'wb', dir=target.parent, prefix=f'.{target.name}.', delete=False
              ) as handle:
                  temporary = Path(handle.name)
                  handle.write(payload)
                  handle.flush()
                  os.fsync(handle.fileno())
              try:
                  os.link(temporary, target)
              except FileExistsError:
                  if target.read_bytes() != payload:
                      raise SystemExit('Legacy-profit runtime evidence raced')
              finally:
                  temporary.unlink(missing_ok=True)
          PY
          PYTHONPATH="${research_root}/src:${research_root}" \
            python "${research_root}/scripts/project_decision_legacy_profit_relative_research.py" \
              --root "${research_root}" \
              --signal-date "${signal_date}" \
              --runtime-features-csv "${canonical_features}"
          SOURCE_ROOT="${research_root}" \
            TARGET_ROOT="${GITHUB_WORKSPACE}" \
            SIGNAL_DATE="${signal_date}" \
            PRESERVED_ACTION_SHA256="${PRESERVED_ACTION_SHA256}" \
            PYTHONPATH="${research_root}/src:${research_root}" \
            python - <<'PY'
          import hashlib
          import hmac
          import json
          import os
          import tempfile
          from pathlib import Path

          from top10decision.decision.legacy_profit_relative_research import (
              OUTPUT_ROOT,
              validate_repository_chain,
          )

          source_root = Path(os.environ['SOURCE_ROOT']).resolve(strict=True)
          target_root = Path(os.environ['TARGET_ROOT']).resolve(strict=True)
          signal_date = os.environ['SIGNAL_DATE']
          if source_root == target_root:
              raise SystemExit('Legacy-profit research source must be isolated')
          evidence = validate_repository_chain(source_root)
          if (
              evidence.get('signal_date') != signal_date
              or evidence.get('deterministic_rebuild_match') is not True
              or evidence.get('public_static_chain_match') is not True
          ):
              raise SystemExit('Legacy-profit deterministic source validation failed')

          projection_json = OUTPUT_ROOT / f'projection_{signal_date}.json'
          projection_csv = OUTPUT_ROOT / f'projection_{signal_date}.csv'
          public_index = OUTPUT_ROOT / 'index.json'
          runtime_evidence = Path(
              f'data/decision/legacy_profit_relative/runtime_features_{signal_date}.csv'
          )
          immutable_relatives = (
              runtime_evidence,
              projection_json,
              projection_csv,
          )
          mutable_relatives = (public_index,)
          exact_relatives = immutable_relatives + mutable_relatives

          def safe_source(relative: Path) -> Path:
              if relative.is_absolute() or '..' in relative.parts:
                  raise SystemExit(f'Legacy-profit source path is unsafe: {relative}')
              current = source_root
              for part in relative.parts:
                  current = current / part
                  if current.is_symlink():
                      raise SystemExit(
                          f'Legacy-profit source has a symlink ancestor: {relative}'
                      )
              if not current.is_file() or current.resolve(strict=True) != current:
                  raise SystemExit(f'Legacy-profit source is missing/escaped: {relative}')
              return current

          def target_path(relative: Path) -> Path:
              current = target_root
              for part in relative.parent.parts:
                  current = current / part
                  if current.exists():
                      if not current.is_dir() or current.is_symlink():
                          raise SystemExit(
                              f'Legacy-profit target ancestor is unsafe: {relative}'
                          )
                  else:
                      current.mkdir()
              path = target_root / relative
              if path.exists() and (not path.is_file() or path.is_symlink()):
                  raise SystemExit(f'Legacy-profit target is unsafe: {relative}')
              return path

          def install_immutable(relative: Path) -> None:
              payload = safe_source(relative).read_bytes()
              target = target_path(relative)
              if target.exists():
                  if target.read_bytes() != payload:
                      raise SystemExit(
                          f'Legacy-profit immutable conflict: {relative}'
                      )
                  return
              with tempfile.NamedTemporaryFile(
                  'wb', dir=target.parent, prefix=f'.{target.name}.', delete=False
              ) as handle:
                  temporary = Path(handle.name)
                  handle.write(payload)
                  handle.flush()
                  os.fsync(handle.fileno())
              try:
                  os.link(temporary, target)
              except FileExistsError:
                  if target.read_bytes() != payload:
                      raise SystemExit(
                          f'Legacy-profit immutable copy raced: {relative}'
                      )
              finally:
                  temporary.unlink(missing_ok=True)

          def replace_pointer(relative: Path) -> None:
              payload = safe_source(relative).read_bytes()
              target = target_path(relative)
              with tempfile.NamedTemporaryFile(
                  'wb', dir=target.parent, prefix=f'.{target.name}.', delete=False
              ) as handle:
                  temporary = Path(handle.name)
                  handle.write(payload)
                  handle.flush()
                  os.fsync(handle.fileno())
              try:
                  os.replace(temporary, target)
              finally:
                  temporary.unlink(missing_ok=True)

          for relative in immutable_relatives:
              install_immutable(relative)
          for relative in mutable_relatives:
              replace_pointer(relative)

          public_evidence = validate_repository_chain(target_root)
          if (
              public_evidence.get('signal_date') != signal_date
              or public_evidence.get('public_static_chain_match') is not True
              or public_evidence.get('deterministic_rebuild_match') is not True
          ):
              raise SystemExit('Copied legacy-profit public chain is invalid')
          private_target = target_root / '.dc20-private'
          if private_target.exists():
              raise SystemExit('Private legacy-profit runtime pool escaped isolation')
          action = target_root / 'outputs/decision/action_plan_latest.json'
          action_digest = hashlib.sha256(action.read_bytes()).hexdigest() if action.is_file() else ''
          if not hmac.compare_digest(
              action_digest,
              os.environ['PRESERVED_ACTION_SHA256'],
          ):
              raise SystemExit('Legacy-profit sidecar modified preserved Action')
          print(json.dumps({
              'status': 'LEGACY_PROFIT_RELATIVE_SIDECAR_COPIED',
              'signal_date': signal_date,
              'copied_paths': [value.as_posix() for value in exact_relatives],
              'private_runtime_copied': False,
              'actual_execution_claimed': False,
          }, ensure_ascii=False, sort_keys=True))
          PY
      - name: Build exact allowlisted Daily candidate patch
        id: candidate
        if: ${{ steps.session.outputs.write_eligible == 'true' }}
        run: |
          set -euo pipefail
          find . -type d -name __pycache__ -prune -exec rm -rf {} +
          find . -type f -name '*.pyc' -delete
          git add -A -- data/pred
          git add -A -- data/market
          git add -A -- docs
          git add -A -- outputs/decision data/decision
          git add -A -- data/decision_executable_profit/forward/selections data/decision_executable_profit/forward/statistics/summary.json
          signal_date="$(tr -d '\r\n' < "${RUNNER_TEMP}/daily-signal-date.txt")"
          git add -A -- "outputs/auction_v3/predictions/pred_${signal_date}.csv"
          git reset -q HEAD -- ':(glob)outputs/decision/action_plan_*.json' 'outputs/decision/report_index.json'
          git diff --cached --no-renames --name-only -z > "${RUNNER_TEMP}/staged_paths.bin"
          git ls-files --stage -z > "${RUNNER_TEMP}/staged_index.bin"
          STAGED_PATHS="${RUNNER_TEMP}/staged_paths.bin" STAGED_INDEX="${RUNNER_TEMP}/staged_index.bin" SIGNAL_DATE_FILE="${RUNNER_TEMP}/daily-signal-date.txt" python - <<'PY'
          import csv, fnmatch, hashlib, json, os, re
          from decimal import Decimal, InvalidOperation
          from pathlib import Path
          allowed=('data/pred/pred_source_latest.csv','data/pred/_pred_source_meta.json','data/pred/archive/**','data/pred/pred_top10_*.csv','data/market/raw/**','data/market/trade_cal_sse.csv','data/market/features_base_*.csv','data/market/features_limit_*.csv','data/market/truth_close_*.csv','data/market/_meta_*.json','docs/weights/**','docs/signals/**','outputs/auction_v3/predictions/pred_20??????.csv','outputs/decision/decision_report_20??????.md','outputs/decision/eval_20??????.json','outputs/decision/forward_observation_protocol_v12.json','outputs/decision/research_context_20??????.json','outputs/decision/research_context_dc20_20??????.json','outputs/decision/three_rank_top10_20??????.json','outputs/decision/three_rank_top10_20??????.csv','outputs/decision/three_rank_index.json','outputs/decision/executable_profit_research/projection_20??????.json','outputs/decision/executable_profit_research/projection_20??????.csv','outputs/decision/executable_profit_research/shadow_statistics_20??????_asof_20??????.json','outputs/decision/executable_profit_research/index.json','outputs/decision/legacy_profit_relative_research/projection_20??????.json','outputs/decision/legacy_profit_relative_research/projection_20??????.csv','outputs/decision/legacy_profit_relative_research/index.json','data/decision_executable_profit/forward/selections/shadow_20??????.json','data/decision_executable_profit/forward/selections/shadow_20??????.csv','data/decision_executable_profit/forward/selections/index.json','data/decision_executable_profit/forward/statistics/summary.json','data/decision/**','docs/reports/*.md')
          forbidden=('outputs/decision/action_plan_*.json','outputs/decision/report_index.json')
          def matches(path, pattern):
              if pattern.endswith('/**'):
                  return path.startswith(pattern[:-3] + '/')
              path_parts = path.split('/')
              pattern_parts = pattern.split('/')
              return len(path_parts) == len(pattern_parts) and all(
                  fnmatch.fnmatchcase(part, expected)
                  for part, expected in zip(path_parts, pattern_parts)
              )
          paths=[p.decode() for p in Path(os.environ['STAGED_PATHS']).read_bytes().split(b'\0') if p]
          signal_date=Path(os.environ['SIGNAL_DATE_FILE']).read_text(encoding='ascii').strip()
          if re.fullmatch(r'20\d{6}', signal_date) is None:
              raise SystemExit('invalid Daily candidate signal date')
          expected_prediction=f'outputs/auction_v3/predictions/pred_{signal_date}.csv'
          bad=[p for p in paths if not any(matches(p,a) for a in allowed) or any(matches(p,a) for a in forbidden)]
          bad.extend(
              p for p in paths
              if p.startswith('outputs/auction_v3/') and p != expected_prediction
          )
          if bad: raise SystemExit(f'non-allowlisted Daily paths staged: {bad}')
          path_set=set(paths)
          index_modes={}
          for entry in Path(os.environ['STAGED_INDEX']).read_bytes().split(b'\0'):
              if not entry:
                  continue
              metadata, raw_path = entry.split(b'\t', 1)
              mode, _object_id, stage = metadata.decode('ascii').split()
              path = raw_path.decode()
              if path not in path_set and path != expected_prediction:
                  continue
              if stage != '0' or path in index_modes:
                  raise SystemExit(f'non-stage-zero Daily index entry: {path}')
              index_modes[path] = mode
          bad_modes=[(path,index_modes[path]) for path in paths if path in index_modes and index_modes[path] != '100644']
          if bad_modes: raise SystemExit(f'non-regular Daily paths staged: {bad_modes}')
          prediction_path=Path(expected_prediction)
          if (
              index_modes.get(expected_prediction) != '100644'
              or prediction_path.is_symlink()
              or not prediction_path.is_file()
              or prediction_path.stat().st_size <= 0
          ):
              raise SystemExit('Daily exact pred_D is missing or unsafe')
          selection_path=Path(
              f'data/decision_executable_profit/forward/selections/'
              f'shadow_{signal_date}.json'
          )
          projection_path=Path(
              f'outputs/decision/executable_profit_research/'
              f'projection_{signal_date}.json'
          )
          if any(
              path.is_symlink() or not path.is_file()
              for path in (selection_path, projection_path)
          ):
              raise SystemExit('Daily exact pred_D binding artifact is unsafe')
          selection=json.loads(selection_path.read_text(encoding='utf-8'))
          projection=json.loads(projection_path.read_text(encoding='utf-8'))
          digest=hashlib.sha256(prediction_path.read_bytes()).hexdigest()
          source_d=selection.get('source_d_feature') or {}
          source_binding=(
              (projection.get('source_bindings') or {}).get('selection') or {}
          )
          selection_rows=selection.get('rows')
          if (
              selection.get('signal_date') != signal_date
              or projection.get('signal_date') != signal_date
              or not isinstance(selection_rows, list)
              or source_d.get('file_name') != prediction_path.name
              or source_d.get('file_sha256') != digest
              or source_d.get('selected_row_count') != len(selection_rows)
              or source_binding.get('d_feature_file_sha256') != digest
          ):
              raise SystemExit('Daily exact pred_D SHA binding drifted')
          with prediction_path.open(
              'r', encoding='utf-8-sig', newline=''
          ) as handle:
              reader=csv.DictReader(handle)
              required={
                  'action', 'selected', 'first_layer_selected',
                  'trade_selected', 'guidance_only', 'broker_connected',
                  'market_order_allowed', 'order_type',
              }
              if not required.issubset(reader.fieldnames or ()):
                  raise SystemExit('Daily exact pred_D action columns are missing')
              prediction_rows=list(reader)
          if not prediction_rows:
              raise SystemExit('Daily exact pred_D candidate pool is empty')
          def exact_integer(value, expected):
              try:
                  return Decimal(str(value).strip()) == expected
              except (InvalidOperation, ValueError):
                  return False
          for row in prediction_rows:
              if (
                  str(row.get('action') or '').strip()
                  not in {'REJECT', 'SHADOW_ONLY'}
                  or not all(
                      exact_integer(row.get(column), 0)
                      for column in (
                          'selected', 'first_layer_selected',
                          'trade_selected', 'broker_connected',
                          'market_order_allowed',
                      )
                  )
                  or not exact_integer(row.get('guidance_only'), 1)
                  or str(row.get('order_type') or '').strip()
                  != 'LIMIT_ONLY_MANUAL'
              ):
                  raise SystemExit('Daily exact pred_D action boundary drifted')
          PY
          git diff --cached --binary --full-index > "${RUNNER_TEMP}/candidate.patch"
          CANDIDATE_PATCH="${RUNNER_TEMP}/candidate.patch" CANDIDATE_SHA="${RUNNER_TEMP}/candidate.patch.sha256" python - <<'PY'
          import hashlib, os
          from pathlib import Path
          patch = Path(os.environ['CANDIDATE_PATCH'])
          Path(os.environ['CANDIDATE_SHA']).write_text(hashlib.sha256(patch.read_bytes()).hexdigest() + '\n', encoding='ascii')
          PY
          if git diff --cached --quiet; then echo 'has_changes=false' >> "${GITHUB_OUTPUT}"; else echo 'has_changes=true' >> "${GITHUB_OUTPUT}"; fi
      - name: Upload immutable candidate patch
        if: ${{ steps.session.outputs.write_eligible == 'true' && steps.mode.outputs.publish == 'true' && steps.candidate.outputs.has_changes == 'true' }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: daily-candidate-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            ${{ runner.temp }}/base_sha.txt
            ${{ runner.temp }}/candidate.patch
            ${{ runner.temp }}/candidate.patch.sha256
            ${{ runner.temp }}/daily-signal-date.txt
          if-no-files-found: error
          retention-days: 1

  # Compatibility jobs intentionally remain visible for workflow-contract tooling.
  # Production publication requires an active freeze, so online learning and the
  # former second writer are fail-closed here; model promotion is an offline flow.
  pfill_learning:
    name: Disabled production learning compatibility gate
    if: ${{ false }}
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - run: echo 'production freeze forbids online learning in the Daily writer'

  decision_refresh_after_learning:
    name: Disabled post-learning refresh compatibility gate
    needs: pfill_learning
    if: ${{ false }}
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - run: echo 'Daily has exactly one validated compute and one CAS publisher'

  publish:
    name: CAS publish one Daily commit
    needs: compute
    if: ${{ needs.compute.outputs.publish == 'true' && needs.compute.outputs.has_changes == 'true' }}
    outputs:
      published_head: ${{ steps.publish.outputs.published_head }}
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout current main without persisted credentials
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          ref: main
          fetch-depth: 1
          persist-credentials: false
      - name: Download validated candidate
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: daily-candidate-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/candidate
      - name: Verify immutable candidate envelope
        env:
          CANDIDATE_DIR: ${{ runner.temp }}/candidate
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib, os, re
          from pathlib import Path
          root = Path(os.environ['CANDIDATE_DIR'])
          expected_files = {
              'base_sha.txt',
              'candidate.patch',
              'candidate.patch.sha256',
              'daily-signal-date.txt',
          }
          if not root.is_dir() or any(path.is_symlink() for path in root.rglob('*')):
              raise SystemExit('candidate envelope is missing or contains a symlink')
          actual_files = {path.relative_to(root).as_posix() for path in root.rglob('*') if path.is_file()}
          if actual_files != expected_files:
              raise SystemExit(f'candidate envelope file set mismatch: {sorted(actual_files)}')
          base_sha = (root / 'base_sha.txt').read_text(encoding='ascii').strip()
          claimed_sha = (root / 'candidate.patch.sha256').read_text(encoding='ascii').strip()
          signal_date = (root / 'daily-signal-date.txt').read_text(
              encoding='ascii'
          ).strip()
          if re.fullmatch(r'[0-9a-f]{40}', base_sha) is None:
              raise SystemExit('invalid candidate base SHA')
          if re.fullmatch(r'[0-9a-f]{64}', claimed_sha) is None:
              raise SystemExit('invalid candidate patch SHA256')
          if re.fullmatch(r'20\d{6}', signal_date) is None:
              raise SystemExit('invalid Daily candidate signal date')
          actual_sha = hashlib.sha256((root / 'candidate.patch').read_bytes()).hexdigest()
          if actual_sha != claimed_sha:
              raise SystemExit('candidate.patch SHA256 mismatch')
          PY
      - name: Apply exact base candidate and create one commit
        run: |
          set -euo pipefail
          expected="$(cat "${RUNNER_TEMP}/candidate/base_sha.txt")"
          test "$(git rev-parse HEAD)" = "${expected}"
          git apply --index --binary "${RUNNER_TEMP}/candidate/candidate.patch"
          git diff --cached --no-renames --name-only -z > "${RUNNER_TEMP}/publish_staged_paths.bin"
          git ls-files --stage -z > "${RUNNER_TEMP}/publish_staged_index.bin"
          PUBLISH_STAGED_PATHS="${RUNNER_TEMP}/publish_staged_paths.bin" PUBLISH_STAGED_INDEX="${RUNNER_TEMP}/publish_staged_index.bin" SIGNAL_DATE_FILE="${RUNNER_TEMP}/candidate/daily-signal-date.txt" python - <<'PY'
          import csv, fnmatch, hashlib, json, os, re
          from decimal import Decimal, InvalidOperation
          from pathlib import Path
          allowed=('data/pred/pred_source_latest.csv','data/pred/_pred_source_meta.json','data/pred/archive/**','data/pred/pred_top10_*.csv','data/market/raw/**','data/market/trade_cal_sse.csv','data/market/features_base_*.csv','data/market/features_limit_*.csv','data/market/truth_close_*.csv','data/market/_meta_*.json','docs/weights/**','docs/signals/**','outputs/auction_v3/predictions/pred_20??????.csv','outputs/decision/decision_report_20??????.md','outputs/decision/eval_20??????.json','outputs/decision/forward_observation_protocol_v12.json','outputs/decision/research_context_20??????.json','outputs/decision/research_context_dc20_20??????.json','outputs/decision/three_rank_top10_20??????.json','outputs/decision/three_rank_top10_20??????.csv','outputs/decision/three_rank_index.json','outputs/decision/executable_profit_research/projection_20??????.json','outputs/decision/executable_profit_research/projection_20??????.csv','outputs/decision/executable_profit_research/shadow_statistics_20??????_asof_20??????.json','outputs/decision/executable_profit_research/index.json','outputs/decision/legacy_profit_relative_research/projection_20??????.json','outputs/decision/legacy_profit_relative_research/projection_20??????.csv','outputs/decision/legacy_profit_relative_research/index.json','data/decision_executable_profit/forward/selections/shadow_20??????.json','data/decision_executable_profit/forward/selections/shadow_20??????.csv','data/decision_executable_profit/forward/selections/index.json','data/decision_executable_profit/forward/statistics/summary.json','data/decision/**','docs/reports/*.md')
          forbidden=('outputs/decision/action_plan_*.json','outputs/decision/report_index.json')
          def matches(path, pattern):
              if pattern.endswith('/**'):
                  return path.startswith(pattern[:-3] + '/')
              path_parts = path.split('/')
              pattern_parts = pattern.split('/')
              return len(path_parts) == len(pattern_parts) and all(
                  fnmatch.fnmatchcase(part, expected)
                  for part, expected in zip(path_parts, pattern_parts)
              )
          paths=[p.decode() for p in Path(os.environ['PUBLISH_STAGED_PATHS']).read_bytes().split(b'\0') if p]
          signal_date=Path(os.environ['SIGNAL_DATE_FILE']).read_text(encoding='ascii').strip()
          if re.fullmatch(r'20\d{6}', signal_date) is None:
              raise SystemExit('invalid Daily publish signal date')
          expected_prediction=f'outputs/auction_v3/predictions/pred_{signal_date}.csv'
          bad=[p for p in paths if not any(matches(p,a) for a in allowed) or any(matches(p,a) for a in forbidden)]
          bad.extend(
              p for p in paths
              if p.startswith('outputs/auction_v3/') and p != expected_prediction
          )
          if bad: raise SystemExit(f'non-allowlisted Daily publish paths staged: {bad}')
          path_set=set(paths)
          index_modes={}
          for entry in Path(os.environ['PUBLISH_STAGED_INDEX']).read_bytes().split(b'\0'):
              if not entry:
                  continue
              metadata, raw_path = entry.split(b'\t', 1)
              mode, _object_id, stage = metadata.decode('ascii').split()
              path = raw_path.decode()
              if path not in path_set and path != expected_prediction:
                  continue
              if stage != '0' or path in index_modes:
                  raise SystemExit(f'non-stage-zero Daily publish index entry: {path}')
              index_modes[path] = mode
          bad_modes=[(path,index_modes[path]) for path in paths if path in index_modes and index_modes[path] != '100644']
          if bad_modes: raise SystemExit(f'non-regular Daily publish paths staged: {bad_modes}')
          prediction_path=Path(expected_prediction)
          if (
              index_modes.get(expected_prediction) != '100644'
              or prediction_path.is_symlink()
              or not prediction_path.is_file()
              or prediction_path.stat().st_size <= 0
          ):
              raise SystemExit('Daily publish exact pred_D is missing or unsafe')
          selection_path=Path(
              f'data/decision_executable_profit/forward/selections/'
              f'shadow_{signal_date}.json'
          )
          projection_path=Path(
              f'outputs/decision/executable_profit_research/'
              f'projection_{signal_date}.json'
          )
          if any(
              path.is_symlink() or not path.is_file()
              for path in (selection_path, projection_path)
          ):
              raise SystemExit(
                  'Daily publish exact pred_D binding artifact is unsafe'
              )
          selection=json.loads(selection_path.read_text(encoding='utf-8'))
          projection=json.loads(projection_path.read_text(encoding='utf-8'))
          digest=hashlib.sha256(prediction_path.read_bytes()).hexdigest()
          source_d=selection.get('source_d_feature') or {}
          source_binding=(
              (projection.get('source_bindings') or {}).get('selection') or {}
          )
          selection_rows=selection.get('rows')
          if (
              selection.get('signal_date') != signal_date
              or projection.get('signal_date') != signal_date
              or not isinstance(selection_rows, list)
              or source_d.get('file_name') != prediction_path.name
              or source_d.get('file_sha256') != digest
              or source_d.get('selected_row_count') != len(selection_rows)
              or source_binding.get('d_feature_file_sha256') != digest
          ):
              raise SystemExit('Daily publish exact pred_D SHA binding drifted')
          with prediction_path.open(
              'r', encoding='utf-8-sig', newline=''
          ) as handle:
              reader=csv.DictReader(handle)
              required={
                  'action', 'selected', 'first_layer_selected',
                  'trade_selected', 'guidance_only', 'broker_connected',
                  'market_order_allowed', 'order_type',
              }
              if not required.issubset(reader.fieldnames or ()):
                  raise SystemExit(
                      'Daily publish exact pred_D action columns are missing'
                  )
              prediction_rows=list(reader)
          if not prediction_rows:
              raise SystemExit('Daily publish exact pred_D candidate pool is empty')
          def exact_integer(value, expected):
              try:
                  return Decimal(str(value).strip()) == expected
              except (InvalidOperation, ValueError):
                  return False
          for row in prediction_rows:
              if (
                  str(row.get('action') or '').strip()
                  not in {'REJECT', 'SHADOW_ONLY'}
                  or not all(
                      exact_integer(row.get(column), 0)
                      for column in (
                          'selected', 'first_layer_selected',
                          'trade_selected', 'broker_connected',
                          'market_order_allowed',
                      )
                  )
                  or not exact_integer(row.get('guidance_only'), 1)
                  or str(row.get('order_type') or '').strip()
                  != 'LIMIT_ONLY_MANUAL'
              ):
                  raise SystemExit(
                      'Daily publish exact pred_D action boundary drifted'
                  )
          if not paths: raise SystemExit('Daily candidate patch became empty')
          PY
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git commit -m 'auto: sync market raw, build fs, update decision outputs'
      - name: Publish exact CAS commit with minimally injected token
        id: publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          expected="$(cat "${RUNNER_TEMP}/candidate/base_sha.txt")"
          git fetch origin main
          test "$(git rev-parse origin/main)" = "${expected}"
          git push "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:main
          echo "published_head=$(git rev-parse HEAD)" >> "${GITHUB_OUTPUT}"

  deploy-pages:
    name: Deploy exact published Daily revision
    needs: publish
    if: ${{ needs.publish.result == 'success' && needs.publish.outputs.published_head != '' }}
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: ./.github/workflows/deploy_dc20_pages.yml
    with:
      expected_head: ${{ needs.publish.outputs.published_head }}
