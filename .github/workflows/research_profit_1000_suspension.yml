name: DC20 · retained suspension evidence probe (read only)

on:
  push:
    branches: [main]
    paths:
      - "work/profit_1000_upgrade/SUSPENSION_REQUEST.json"

permissions:
  contents: read

concurrency:
  group: dc20-profit-1000-suspension-probe
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  probe:
    if: github.repository == 'njedu2023-prog/DC20' && github.ref == 'refs/heads/main' && github.run_attempt == 1
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - name: Checkout exact source without write credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Python standard library only
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.12.13"
      - name: Test exact diagnostic without credentials or network
        run: python -m unittest discover -s work/profit_1000_upgrade -p 'test_suspension_probe.py' -v
      - name: Check five exact daily gaps and suspension evidence (maximum ten calls)
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        run: >-
          python work/profit_1000_upgrade/suspension_probe.py
          --output "${RUNNER_TEMP}/dc20-profit-1000-suspension"
      - name: Verify checkout unchanged
        if: always()
        run: test -z "$(git status --porcelain=v1)"
      - name: Save positive and missing evidence without modifying labels
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: dc20-profit-1000-suspension-${{ github.sha }}-${{ github.run_id }}
          path: ${{ runner.temp }}/dc20-profit-1000-suspension
          if-no-files-found: warn
          retention-days: 30
